.DS_Store

# Node
node_modules/
*.log

# Build output
client/dist/
client/release/

# The Electron binary is downloaded into node_modules at install time (~100MB
# per platform) and must never be committed, even if node_modules is ever
# force-added for vendoring.
client/node_modules/electron/dist/

# TypeScript incremental build state
*.tsbuildinfo
client/build/icon.iconset/
